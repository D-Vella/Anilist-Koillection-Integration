# Copy this file to .env and fill in your own values. .env is git-ignored.

# --- Koillection ---------------------------------------------------------

# Base URL of your Koillection instance (no trailing slash, no /api suffix)
KOILLECTION_BASE_URL=http://192.168.1.50:8080

# Credentials for a Koillection user account
KOILLECTION_USERNAME=your-username
KOILLECTION_PASSWORD=your-password

# Name (or UUID) of the parent Koillection collection whose direct children
# are one sub-collection per series, e.g. "Manga"
KOILLECTION_COLLECTION=Manga

# Label of the custom field used to store the AniList description on each series collection
KOILLECTION_DESCRIPTION_LABEL=Description

# Label of the custom field used to pin/record the AniList id for a series.
# If you set this field by hand on a series collection (e.g. when a title
# search matches the wrong series), that id is used directly and never
# searched for again.
KOILLECTION_ANILIST_ID_LABEL=AniList ID

# --- AniList ---------------------------------------------------------------

# AniList GraphQL endpoint (rarely needs changing)
ANILIST_API_URL=https://graphql.anilist.co

# Seconds to wait between AniList requests to stay within its rate limit
ANILIST_REQUEST_DELAY=2.0

# --- Behaviour ---------------------------------------------------------------

# Set to "true" to overwrite series collections that already have an image/description
OVERWRITE_EXISTING=false

# Set to "true" to log what would change without writing anything
DRY_RUN=false

# HTTP request timeout in seconds
REQUEST_TIMEOUT=15
